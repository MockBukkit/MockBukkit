name: Tag

on:
  push:
    branches: [ minecraft/v* ]

permissions:
  contents: read

# Serialize releases per branch: if two PRs merge close together, the second
# Tag run must wait for the first to finish pushing its tag and publishing,
# otherwise both read the same "latest tag" and compute a colliding version.
# cancel-in-progress: false — never cancel a release mid-publish; queue instead.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  tag:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 15
    env:
      TAG_PREFIX: "MC26.2"
    permissions:
      contents: write
      pull-requests: read
    outputs:
      version: ${{ steps.format-tag.outputs.full_tag }}
      publish_version: ${{ steps.format-tag.outputs.publish_version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Get merged pull request
        uses: actions-ecosystem/action-get-merged-pull-request@59afe90821bb0b555082ce8ff1e36b03f91553d9 # v1.0.1
        if: ${{ github.event_name == 'push' }}
        id: pr
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get release label
        uses: actions-ecosystem/action-release-label@1c7ef83d383a57bf253a2e2e9dfe1b55f445ae15 # v1.2.0
        if: ${{ steps.pr.outputs.labels != null }}
        id: release-label
        with:
          label_prefix: 'release/'
          labels: ${{ steps.pr.outputs.labels }}

      - name: Get latest tag
        if: ${{ steps.release-label.outputs.level != null }}
        id: get-latest-tag
        run: |
          # 1. Try finding the latest tag with TAG_PREFIX on the current branch
          LATEST_TAG=$(git tag -l --merged HEAD --sort=-v:refname "${TAG_PREFIX}*" | head -n 1)

          # 2. Fallback: Match exact 'vX.Y.Z' and convert to TAG_PREFIX-vX.Y.Z
          if [ -z "$LATEST_TAG" ]; then
            echo "No prefix tag found on this branch. Searching for exact vX.Y.Z tag..."
            RAW_TAG=$(git tag -l --merged HEAD --sort=-v:refname "v*" | grep -m 1 -E '^v[0-9]+\.[0-9]+\.[0-9]+$')

            if [ -n "$RAW_TAG" ]; then
              LATEST_TAG="${TAG_PREFIX}-${RAW_TAG}"
            fi
          fi

          # 3. Default Fallback if no tags exist at all
          if [ -z "$LATEST_TAG" ]; then
            echo "No valid tags found on this branch. Using default fallback..."
            LATEST_TAG="${TAG_PREFIX}-v0.0.0"
          fi

          # 4. Extract clean SemVer (e.g., "MC26.2-v4.116.1" -> "4.116.1")
          CLEAN_VERSION=$(echo "$LATEST_TAG" | sed 's/.*-v//; s/^v//')

          echo "Found tag $LATEST_TAG (version: $CLEAN_VERSION)"

          # Export both outputs
          echo "tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "version=$CLEAN_VERSION" >> $GITHUB_OUTPUT

      - name: Set up JDK 25
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        if: ${{ steps.release-label.outputs.level != null }}
        with:
          java-version: 25
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        if: ${{ steps.release-label.outputs.level != null }}

      - name: Read Paper version from gradle.properties
        id: paper-version
        if: ${{ steps.release-label.outputs.level != null }}
        run: |
          # grep keeps only the "Paper ...:" lines: the gradle wrapper writes its
          # "Downloading ...10%...100%" distribution-bootstrap progress to stdout on a
          # cold runner, which would otherwise land in the release tag message. Filtering
          # by line content strips it regardless of stream. (No 2>&1 — keep stderr out too.)
          {
              echo "paper_details<<PAPER_DETAILS_EOF"
              ./gradlew printPaperDetails --quiet --console=plain | grep '^Paper'
              echo "PAPER_DETAILS_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Bump version
        uses: actions-ecosystem/action-bump-semver@34e334551143a5301f38c830e44a22273c6ff5c5 # v1.0.0
        if: ${{ steps.release-label.outputs.level != null }}
        id: bump-semver
        with:
          current_version: ${{ steps.get-latest-tag.outputs.version }}
          level: ${{ steps.release-label.outputs.level }}

      - name: Format new tag and versions
        id: format-tag
        if: ${{ steps.bump-semver.outputs.new_version != null }}
        env:
          NEW_VERSION: ${{ steps.bump-semver.outputs.new_version }}
        run: |
          # Clean raw version (e.g., "4.116.2")
          CLEAN_VER="${NEW_VERSION#v}"

          # Full tag with prefix (e.g., "MC26.2-v4.116.2")
          FULL_TAG="${TAG_PREFIX}-v${CLEAN_VER}"

          echo "full_tag=$FULL_TAG" >> "$GITHUB_OUTPUT"
          echo "publish_version=$CLEAN_VER" >> "$GITHUB_OUTPUT"

      - name: Push tag
        uses: actions-ecosystem/action-push-tag@6e82caefe706f5a729e354df7443dc82f98a414f # v1.0.0
        if: ${{ steps.format-tag.outputs.full_tag != null }}
        with:
          tag: ${{ steps.format-tag.outputs.full_tag }}
          message: |
            ${{ steps.format-tag.outputs.full_tag }}
            Triggered by: PR #${{ steps.pr.outputs.number }}
            Commit: ${{ github.sha }}
            ---
            ${{ steps.paper-version.outputs.paper_details }}

  call-publish-workflow:
    name: Call publish workflow
    uses: ./.github/workflows/publish.yml
    needs: tag
    if: ${{ needs.tag.outputs.version != null }}
    with:
      version: ${{ needs.tag.outputs.publish_version }}
    secrets:
      OSSRH_USERNAME: ${{ secrets.OSSRH_USERNAME }}
      OSSRH_TOKEN: ${{ secrets.OSSRH_TOKEN }}
      KEY_PASS: ${{ secrets.KEY_PASS }}
      PRIV_KEY: ${{ secrets.PRIV_KEY }}
